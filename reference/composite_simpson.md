# Composite Simpson's rule

Composite Simpson's rule

## Usage

``` r
composite_simpson(values, grid)
```

## Arguments

- values:

  List of function values at grid points (each is a vector)

- grid:

  Numeric vector of grid points

## Value

Integral approximation (vector of same length as each element in values)
