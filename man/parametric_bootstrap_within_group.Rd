% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parametric_bootstrap.R
\name{parametric_bootstrap_within_group}
\alias{parametric_bootstrap_within_group}
\title{Parametric bootstrap for a within-group SensIAT model
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}}
\usage{
parametric_bootstrap_within_group(
  within_group_model,
  nboot = 100,
  simulate_args = list(),
  seed = NULL,
  progress = interactive(),
  sample_coefficients = FALSE,
  refit = TRUE,
  return = c("coefficients", "models", "data"),
  prune_models = FALSE,
  gc_every = 10L,
  verbosity = c("none", "basic", "detailed"),
  verbose = NULL
)
}
\arguments{
\item{within_group_model}{A fitted \code{SensIAT_within_group_model} object.}

\item{nboot}{Number of bootstrap replicates.}

\item{simulate_args}{List of arguments to pass to \code{simulate_SensIAT_data()}.
If not specified, \code{End}, \code{n_subjects}, \code{initial_outcome_mean}, and
\code{initial_outcome_sd} are inferred from the fitted model.}

\item{seed}{Optional seed for reproducibility.}

\item{progress}{Logical; show progress bar when available.}

\item{sample_coefficients}{Logical; if \code{TRUE}, sample coefficients from an
asymptotic multivariate normal distribution when \code{vcov()} is available. If \code{FALSE} (default), use
original fitted coefficients.}

\item{refit}{Logical; if \code{TRUE} (default), fit a \code{SensIAT_within_group_model}
on each simulated replicate using the original model's settings.}

\item{return}{One of \code{"coefficients"} (default), \code{"models"}, or \code{"data"}.
\code{"coefficients"} is memory-efficient and stores only replicated
marginal mean coefficients.}

\item{prune_models}{Logical; when \code{return = "models"}, prune each replicated
model before returning.}

\item{gc_every}{Integer. Run \code{gc(FALSE)} every \code{gc_every} replications.
Use \code{NULL} to disable explicit garbage collection.}

\item{verbosity}{Logging verbosity for bootstrap internals: one of
\code{"none"}, \code{"basic"}, or \code{"detailed"}.}

\item{verbose}{Deprecated shortcut; if \code{TRUE}, equivalent to
\code{verbosity = "detailed"}.}
}
\value{
If \code{return = "coefficients"}, a
\code{SensIAT_withingroup_bootstrap_results} object. Otherwise returns a
list of replicated fitted models (\code{"models"}) or simulated datasets
(\code{"data"}).
}
\description{
Parametric bootstrap for a within-group SensIAT model
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\examples{
\dontrun{
data("SensIAT_example_data", package = "SensIAT")

# Fit a single-index outcome model on a small subset of the example data.
small_data <- dplyr::filter(
  SensIAT_example_data,
  Subject_ID \%in\% head(unique(SensIAT_example_data$Subject_ID), 8)
)

model <- fit_SensIAT_within_group_model(
  group.data = small_data,
  outcome_modeler = fit_SensIAT_single_index_fixed_coef_model,
  alpha = 0,
  id = Subject_ID,
  outcome = Outcome,
  time = Time,
  End = 830,
  knots = c(60, 260, 460)
)

# This example may take a long time because it fits a single-index outcome model
# and generates bootstrap replicates.
res <- parametric_bootstrap_within_group(
  nboot = 2,
  within_group_model = model,
  simulate_args = list(
    n_subjects = 3,
    End = 5,
    max_visits = 5
  ),
  seed = 123,
  refit = TRUE
)

print(res[[1]])
}
}
