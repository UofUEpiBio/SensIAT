% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extrapolate_from_last_observation.R
\name{extrapolate_from_last_observation_multiple}
\alias{extrapolate_from_last_observation_multiple}
\title{Extrapolate Multiple Time Points from Last Observation}
\usage{
extrapolate_from_last_observation_multiple(
  target_times,
  data,
  time_var,
  slopes = NULL,
  strict = TRUE
)
}
\arguments{
\item{target_times}{Numeric vector. Multiple time points at which to extrapolate values.}

\item{data}{A data.frame containing the observations, with one row per time point.
Must be sorted by the time variable.}

\item{time_var}{Character string. The name of the time variable in \code{data}.}

\item{slopes}{A named numeric vector. Names should match column names in \code{data}
for variables to extrapolate, and values are the slopes (rate of change per unit time).
Variables not in \code{slopes} will be carried forward unchanged from the last observation.}

\item{strict}{Logical. If \code{TRUE} (default), requires that \code{target_time} is greater than
or equal to the last observed time. If \code{FALSE}, allows extrapolation to earlier times.}
}
\value{
A data.frame with one row per target time, containing extrapolated values.
}
\description{
Vectorized version of \code{\link[=extrapolate_from_last_observation]{extrapolate_from_last_observation()}} that extrapolates
to multiple time points at once.
}
\examples{
\dontrun{
df <- data.frame(
    time = c(0, 1, 2, 3),
    x1 = c(10, 12, 14, 16),
    x2 = c(5, 5.5, 6, 6.5)
)

extrapolate_from_last_observation_multiple(
    target_times = c(3.5, 4, 5),
    data = df,
    time_var = "time",
    slopes = c(x1 = 2, x2 = 0.5)
)
}

}
\keyword{internal}
